import React from 'react';

import NoteIndexHeader from './note_index_header.jsx';

const NoteIndex = (props) => (
  <section className="note-index">
    <NoteIndexHeader />

  </section>
)

export default NoteIndex;

// for props.notes.each render NoteIndexItem
